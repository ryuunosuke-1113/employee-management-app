version: '3'

services:

  app:
    build: ./docker
    volumes:
      - ./src:/var/www
    ports:
      - 8000:8000

  db:
    image: mysql:8
    environment:
      MYSQL_DATABASE: laravel
      MYSQL_ROOT_PASSWORD: root
    ports:
      - 3306:3306